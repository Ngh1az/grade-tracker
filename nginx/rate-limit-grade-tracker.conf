# /etc/nginx/conf.d/rate-limit-grade-tracker.conf
# limit_req_zone phải khai báo ở scope http{}, không thể nằm trong server{} của site,
# nên tách riêng file trong conf.d để nginx load ở đúng scope.
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
